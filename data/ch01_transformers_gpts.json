{
  "id": "ch01",
  "roman": "I",
  "title": "W1: Introduction, Transformers and GPTs",
  "blurb": "Highest priority. Every available FoFM exam asks about transformer dimensions, attention FLOPs, autoregressive inference, or the $\\sqrt{d}$ scaling. Treat this as a must-master chapter.",
  "knowledge_points": [
    {
      "id": "attention-compute",
      "title": "Scaled Dot-Product Attention and FLOPs",
      "recap": "Know the single-head attention formula, the shapes of Q, K, V, and why attention cost grows quadratically with context length. For $Q,K \\in \\mathbb{R}^{c \\times d}$, $QK^\\top$ creates a c x c matrix; the dot products dominate compute.",
      "questions": [
        {
          "type": "open",
          "freq": 3,
          "sources": [
            "SS24 P1.3",
            "WS26 P1.1",
            "SS25 P2.2"
          ],
          "q": "Consider a single-head attention layer with input $X \\in \\mathbb{R}^{c \\times d}$, where $c$ is the context length and $d$ is the embedding dimension.\n\n1. Write down the single-head attention mechanism and, given your equation, explain how the number of floating point operations (FLOPs) scales with context length $c$. (2 points)",
          "answer": "**Single-head attention is defined as (1 point):**\n\n$$\\operatorname{Attention}(Q,K,V)=\\operatorname{softmax}\\!\\left(\\frac{QK^\\top}{\\sqrt d}\\right)V$$\n\nwhere $Q,K,V \\in \\mathbb{R}^{c\\times d}$. **The compute cost is dominated by $QK^\\top$**, which gives a $c\\times c$ matrix, and hence **the number of FLOPs scales quadratically with context length $c$ (1 point)**.",
          "extend": "Memory: write the formula first, then immediately say \"$QK^\\top$ is $c\\times c$\". For the SS24 FLOP-counting variant, each of the $C^2$ entries costs about $2D$ FLOPs, so the total is $2C^2D$.",
          "tags": [
            "Attention",
            "FLOPs",
            "Transformer"
          ]
        },
        {
          "type": "open",
          "freq": 3,
          "sources": [
            "SS24 P1.4",
            "WS26 P1.2"
          ],
          "q": "Consider a key and query vector $q,k\\in\\mathbb{R}^d$ whose entries are i.i.d. $\\sim N(0,1)$.\n\n2. What is the variance of $q^\\top k$, and how does this motivate the $1/\\sqrt d$ scaling factor in attention? (4 points)",
          "answer": "**The variance of $q^\\top k$ is (2 points):**\n\n$$\\operatorname{Var}[q^\\top k]=\\sum_{i=1}^d \\operatorname{Var}[q_i k_i]=\\sum_{i=1}^d\\left(E[q_i^2]E[k_i^2]-E[q_i]^2E[k_i]^2\\right)=\\sum_{i=1}^d(1\\cdot1-0\\cdot0)=d.$$\n\n**The variance scales with the embedding dimension $d$**, meaning that for large $d$ the dot products can become very large, **causing vanishing gradients of the softmax (1 point)**. Scaling by $1/\\sqrt d$ gives\n\n$$\\operatorname{Var}\\!\\left[\\frac{1}{\\sqrt d}q^\\top k\\right]=\\frac{1}{d}\\cdot d=1,$$\n\n**keeping the variance constant regardless of $d$ (1 point)**.",
          "extend": "Memory: full credit needs the variance derivation and the conclusion \"variance independent of $d$\". Only saying \"softmax is more stable\" is usually partial credit.",
          "tags": [
            "Attention",
            "Softmax",
            "Variance"
          ]
        },
        {
          "type": "open",
          "freq": 2,
          "sources": [
            "WS26 P1.3"
          ],
          "q": "3. Does training a single-head attention layer with a causal mask require more, the same, or fewer FLOPs than training without a causal mask? Explain your answer and state any assumptions made. (1 point)",
          "answer": "**Depending on the implementation:**\n\n**Same FLOPs:** The full $c\\times c$ attention matrix $QK^\\top$ is computed first, and the causal mask is applied by adding $-\\infty$ to the upper triangle before softmax. The total FLOPs are still dominated by the matrix multiplications, so the masking adds negligible FLOPs compared to the dominant $O(c^2d)$ term.\n\n**Fewer FLOPs:** In optimized implementations such as FlashAttention, the upper triangular entries are never computed. Since roughly half the attention matrix is masked, **the number of FLOPs for the attention computation is roughly halved**.",
          "extend": "Memory: state your assumption. \"Same\" is correct for standard dense attention; \"fewer\" is correct for optimized kernels that skip masked entries.",
          "tags": [
            "Causal mask",
            "FLOPs"
          ]
        }
      ]
    },
    {
      "id": "gpt-inference",
      "title": "Decoder-Only GPT Inference",
      "recap": "GPT-style models are autoregressive: they predict next-token logits and then append one generated token at a time. Inference questions focus on logits shape, KV cache, and padding.",
      "questions": [
        {
          "type": "open",
          "freq": 2,
          "sources": [
            "SS25 P2.1"
          ],
          "q": "Consider a decoder-only autoregressive transformer model with embedding dimension $d$. Assume all questions are about inference time, where the model generates output tokens one by one in an autoregressive manner.\n\n1. Given a vocabulary size $v$ and a sequence length $L$, what are the dimensions of the logits tensor, i.e. the output of the transformer's final linear layer before the softmax, for a single input sequence? (1 point)",
          "answer": "At inference time, the transformer processes a sequence of length $L$ and computes a hidden state at each position. The final linear layer maps these hidden states, shape $L\\times d$, to logits over the vocabulary, resulting in **a logits tensor of shape $L\\times v$ (1 point)**.",
          "extend": "Memory: one row per sequence position, one column per vocabulary item.",
          "tags": [
            "GPT",
            "Logits",
            "Inference"
          ]
        },
        {
          "type": "open",
          "freq": 2,
          "sources": [
            "SS25 P2.2"
          ],
          "q": "Key-value caching is often used to speed up inference by avoiding recomputing keys and values for previous tokens. Consider a single attention head and assume that the key vectors $k_1,\\ldots,k_{n-1}$ for the first $n-1$ tokens are cached. To generate the $(n+1)$th token, the attention layer computes a query vector $q_n\\in\\mathbb{R}^d$ and a key vector $k_n\\in\\mathbb{R}^d$ from the embedding of the last generated token $x_n\\in\\mathbb{R}^d$. It then computes attention scores as $s_i=q_n^\\top k_i$ for $i=1,\\ldots,n$.\n\n(a) For a single attention head, how many FLOPs are required to compute the query and key vectors needed for the attention scores of the $(n+1)$th token? (2 points)\n\n(b) For a single attention head, how many FLOPs are required to compute all $n$ attention scores for the $(n+1)$th token? (2 points)",
          "answer": "**(a)** Each layer computes\n\n$$q_n=W_Qx_n,\\quad k_n=W_Kx_n,\\quad W_Q,W_K\\in\\mathbb{R}^{d\\times d}.$$\n\nEach matrix-vector multiplication requires $d^2$ multiplications and $d(d-1)$ additions, totaling $2d^2-d$ FLOPs. So computing both $q_n$ and $k_n$ costs\n\n$$2\\cdot(2d^2-d)=4d^2-2d\\text{ FLOPs}.$$\n\n**$4d^2$ also gives full points.**\n\n**(b)** Each dot product $q_n^\\top k_i$ requires $d$ multiplications and $d-1$ additions, totaling $2d-1$ FLOPs. To compute all $n$ attention scores:\n\n$$n(2d-1)\\text{ FLOPs}.$$\n\n**$2dn$ also gives full points.**",
          "extend": "Memory: caching avoids recomputing old keys; you still compute the new $q_n,k_n$ and then $n$ dot products against cached/current keys.",
          "tags": [
            "KV cache",
            "Inference",
            "FLOPs"
          ]
        },
        {
          "type": "open",
          "freq": 2,
          "sources": [
            "SS25 P2.3",
            "SS25 P2.4"
          ],
          "q": "3. What is the memory requirement in bytes for caching the key matrices during inference, assuming a transformer with $n_{layers}$ layers, a batch size of $b$, a context length of $c$, and that each entry is stored in 16-bit precision? Assume key matrices are cached for all layers. (1 point)\n\n4. In practice, inference is often performed in batches to speed up computation by processing multiple sequences in parallel. Sequences in a batch are padded with a special PAD token to match the maximum length $L_{max}$. Should sequences be padded on the left or right so that the output logits match those from processing each sequence without batching? Briefly explain your answer. (2 points)",
          "answer": "**3.** Since the output sequence can be up to the maximum context length $c$, the worst-case memory usage for caching the key matrix is\n\n$$\\mathbf{2\\cdot n_{layers}\\cdot d\\cdot c\\cdot b}\\text{ bytes.}$$\n\nThe factor 2 is for 16-bit precision.\n\n**4.** **We left-pad each sequence to length $L_{max}$ (1 point).** Left-padding ensures that the final position corresponds to the final real token of the prompt. If we used right-padding, the last position would contain a PAD token, so even with masking, the model would sample the next token based on logits of a padding token, which it was not trained for. **Correct justification gives the second point.**",
          "extend": "Memory: default decoder generation takes logits from the last position. Left padding keeps the last position real; right padding needs custom \"last non-PAD\" indexing.",
          "tags": [
            "KV cache",
            "Padding",
            "Memory"
          ]
        }
      ]
    },
    {
      "id": "gpt-parameter-compute",
      "title": "GPT Parameter Count and Training Compute",
      "recap": "Tutorial Week 1 asks for non-embedding parameter counts and FLOPs per token. The core result is $N=12n_{layers}d_{embd}^2$ when $d_{ff}=4d_{embd}$, and training compute is about $6N$ in the usual large-embedding regime.",
      "questions": [
        {
          "type": "open",
          "freq": 1,
          "sources": [
            "Tutorial: Week1 Table 1"
          ],
          "q": "Table 1 summarizes the number of parameters and FLOPs per token for the main operations in a decoder-only transformer architecture. Explain how to read this table and which rows are used for the non-embedding parameter count and compute estimate.",
          "figure": {
            "src": "tutorial-week1-table1-gpt-compute.png",
            "alt": "Tutorial Week 1 Table 1: transformer parameters and FLOPs per token.",
            "caption": "Tutorial Week 1, Table 1: General overview of number of parameters and FLOPs per token for different operations in a transformer architecture."
          },
          "answer": "**This table is the shortcut for GPT parameter and compute questions.** Embedding and unembedding rows are shown for completeness, but the usual exam/tutorial question asks for **non-embedding** quantities.\n\nFor the non-embedding parameter count, use the rows for **Q/K/V multiplication**, **attention projection**, and **MLP**:\n\n$$N=n_{layers}(3d_{embd}^2+d_{embd}^2+2d_{embd}d_{ff})=2n_{layers}d_{embd}(2d_{embd}+d_{ff}).$$\n\nWith $d_{ff}=4d_{embd}$, this becomes **$N=12n_{layers}d_{embd}^2$**.\n\nFor forward compute, the table shows that the projection and MLP terms contribute about **$2N$ FLOPs per token**, plus the context-dependent attention term. Under the usual regime where that context term is negligible, training compute is approximately **$6N$ FLOPs per token** because backward costs about two forward passes.",
          "extend": "Memory: do not include input embedding or output embedding when the question asks for non-embedding parameters. The rows to remember are QKV + attention projection + MLP; the total row gives the compact formula.",
          "tags": [
            "GPT",
            "Parameter count",
            "FLOPs"
          ]
        },
        {
          "type": "open",
          "freq": 1,
          "sources": [
            "Tutorial: Week1 Homework Q1"
          ],
          "q": "Number of parameters and compute of the GPT model. Consider the decoder-only transformer considered in class. Omit sub-leading terms such as non-linearities, biases, and normalization. Parameters are `vocab size`, `dim embd`, `n heads`, `n layers`, and `context length`.\n\n1. Compute the total number of non-embedding trainable parameters $N$.\n2. Compute the non-embedding FLOPs per token for a forward pass, using $2ijk$ FLOPs for multiplying an $i\\times j$ matrix by a $j\\times k$ matrix.\n3. If backward costs roughly two forward passes and $d_{embd}\\gg \\text{context length}/12$, justify that training compute per token is roughly $6N$.",
          "answer": "**How to read Table 1:** embedding and unembedding rows are shown for completeness, but the question asks for **non-embedding** parameters and compute. Therefore, for $N$ we add only Q/K/V, attention projection, and MLP parameters.\n\n**Parameter count:** per layer, Q/K/V projections contribute $3d_{embd}^2$, attention output projection contributes $d_{embd}^2$, and the MLP contributes $2d_{embd}d_{ff}$. Across $n_{layers}$ layers,\n\n$$N=n_{layers}(4d_{embd}^2+2d_{embd}d_{ff})=2n_{layers}d_{embd}(2d_{embd}+d_{ff}).$$\n\nWith the usual transformer choice $d_{ff}=4d_{embd}$, this becomes **$N=12n_{layers}d_{embd}^2$**.\n\n**Forward FLOPs from the table:** the Q/K/V, attention projection, and MLP rows together give **$2N$ FLOPs per token**. The attention value computation adds the context-dependent term, so the non-embedding forward compute is\n\n$$2N+2n_{layers}\\cdot context\\_length\\cdot d_{embd}.$$\n\nSome versions of the table count both score computation and value mixing as $2n_{layers}\\cdot2\\,context\\_length\\cdot d_{embd}$; this is the same idea: it is the attention term that depends on context length.\n\n**Training compute:** if $d_{embd}\\gg \\text{context length}/12$, the context-dependent attention term is small compared with $2N$. A forward pass is therefore about $2N$, and backward costs about two forward passes, so training costs **$2N+2\\cdot2N=6N$ FLOPs per token**.",
          "extend": "Memory: Table 1 is the shortcut. Non-embedding parameters come from QKV + attention projection + MLP. Forward non-embedding compute is about $2N$ plus an attention context term; training compute is about $6N$ when the context term is negligible.",
          "tags": [
            "GPT",
            "Parameter count",
            "FLOPs"
          ]
        }
      ]
    }
  ]
}
